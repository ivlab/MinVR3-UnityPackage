# To use the MinVR3 library in your cmake project, add the following to your CMakeLists.txt:
#    find_package(MinVR3 REQUIRED)
#    ... then, after add_executable(${PROJECT_NAME}) or add_library(${PROJECT_NAME})
#    target_link_libraries(${PROJECT_NAME} PUBLIC MinVR3)


include("${CMAKE_CURRENT_LIST_DIR}/MinVR3Targets.cmake")
