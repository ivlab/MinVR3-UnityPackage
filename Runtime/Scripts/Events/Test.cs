using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace IVLab.MinVR3
{

    public class Test : MonoBehaviour
    {
        public VREventPrototypeAny e0;
        public VREventPrototype e;
        public VREventPrototype<float> e1;
        public VREventPrototype<Vector2> e2;
        public VREventPrototype<Vector3> e3;


        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }

    }
}